(require 'ditz)
; slightl different name for the directory with bugs/issues
(setq ditz-issue-directory ".bugs")
; find automatically the bugs directory from the current dir upwards
(setq ditz-find-issue-directory-automatically-flag t)
